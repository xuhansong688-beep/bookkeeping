# 产品设计文档：Bookkeeping MVP

版本：v1.0  
状态：草案  
更新时间：2026-09-10  

## 1. 文档说明

### 1.1 文档目的

本文档定义 Bookkeeping MVP 的产品目标、用户场景、页面结构、核心流程、数据模型、交互规则和验收标准。

### 1.2 文档范围

本文档仅覆盖 `MVP_SCOPE.md` 中定义的 MVP 功能。第二阶段和第三阶段功能仅作方向性描述，不作为当前交付标准。

### 1.3 设计原则

1. 降低记账摩擦，优先保证手动和图片两条流程顺畅。
2. 数据本地优先，用户对数据拥有完全控制权。
3. 图片识别只是辅助，最终保存前必须由用户确认。
4. 页面以移动端为默认设计基准，兼顾桌面浏览器。
5. 视觉风格安静、清晰、偏工具化，不做营销型界面。

## 2. 产品定义

### 2.1 产品定位

Bookkeeping MVP 是一款面向个人的轻量记账工具，重点解决两个问题：

- 日常收支记录是否足够快
- 小票、账单等图片是否能降低录入成本

### 2.2 目标用户

主要用户：习惯使用手机记录日常消费和个人收入的个人用户。

典型场景：

- 饭后快速记录一笔餐饮支出
- 拍下购物小票，自动提取金额和商户
- 月底查看本月收入和支出情况
- 发现记录错误后快速修改或删除

### 2.3 核心价值

- 一笔手动账可以在 30 秒内完成
- 图片账单可以自动生成待确认表单，用户只需检查修改
- 当月结余和收支总额一眼可见
- 数据保存在本地，不依赖登录和网络

## 3. 产品目标

### 3.1 MVP 目标

- 验证手动记账是否满足日常记录需求
- 验证图片识别能否稳定降低录入成本
- 建立可扩展的交易和图片数据模型

### 3.2 成功指标

- 手动新增账单平均完成时间不超过 30 秒
- 图片账单从上传到保存不超过 60 秒
- 图片识别结果被用户直接确认或少量修改后保存的比例足够高
- 用户能够理解并独立完成首次记账

### 3.3 非目标

MVP 不包含：

- 多币种和汇率转换
- 账户管理和账户余额
- 预算和周期账单
- 云同步、登录和多设备
- 银行流水导入
- 复杂报表和统计图表

## 4. 产品形态假设

### 4.1 默认产品形态

本版默认采用本地优先的 Web/PWA 应用：

- 手机浏览器和桌面浏览器均可使用
- 数据默认存储在本地
- 支持离线查看和新增账单
- 图片识别依赖网络服务，网络不可用时自动降级为手动录入

### 4.2 MVP 边界决策

| 项目 | MVP 决策 |
| --- | --- |
| 用户体系 | 单用户，无登录 |
| 数据存储 | 本地持久化 |
| 本位币 | 人民币 CNY |
| 分类 | 仅使用内置分类 |
| 账户 | 不设置账户维度 |
| 图片 | 每笔账单最多关联一张图片 |
| 汇率 | 不提供 |
| 云同步 | 不提供 |

## 5. 信息架构

MVP 包含以下主要页面：

1. 账本首页
2. 手动记账页
3. 图片记账页
4. 账单详情与编辑页

分类和设置页面暂不作为独立功能页。

## 6. 页面设计

### 6.1 账本首页

页面目标：查看本月账单和收支总览，并提供快捷记账入口。

页面结构：

- 顶部月份标题
- 本月结余
- 本月收入
- 本月支出
- 当月账单列表
- 新建账单入口

账单列表项展示：

- 分类图标
- 分类名称
- 备注或商户名称
- 日期
- 金额

列表按日期倒序排列，同一天内按创建时间倒序排列。

交互规则：

- 点击账单进入详情
- 点击新建入口弹出“手动记账”和“图片记账”选择
- 无账单时展示空状态和新建入口
- 支出金额使用醒目颜色，收入金额使用正向颜色

### 6.2 手动记账页

页面目标：快速完成一笔收入或支出。

必填字段：

| 字段 | 规则 |
| --- | --- |
| 类型 | 收入或支出，默认支出 |
| 金额 | 大于 0，最多两位小数 |
| 日期 | 默认今天 |
| 分类 | 必须选择一项 |

选填字段：

| 字段 | 规则 |
| --- | --- |
| 备注 | 最长 200 字 |
| 图片 | 可选一张图片 |

交互规则：

- 进入页面时金额输入框自动聚焦
- 切换类型后自动过滤可用分类
- 提交前校验必填字段
- 保存成功后回到首页并刷新当月统计
- 保存失败时保留用户输入，避免数据丢失

### 6.3 图片记账页

页面目标：上传图片，自动提取字段，由用户确认后生成账单。

图片支持：

- 拍照
- 从相册选择
- JPEG、PNG 格式
- 单张图片不超过 20 MB

识别流程：

1. 用户选择图片
2. 系统进行压缩和格式校验
3. 系统调用识别服务
4. 页面展示识别中的加载状态
5. 识别结果回填到可编辑表单
6. 用户确认或修改
7. 用户保存账单和图片

需要回填的字段：

- 金额
- 日期
- 商户名称
- 分类建议
- 识别置信度

识别失败或置信度过低时：

- 显示图片预览
- 保留图片
- 切换为手动补录表单
- 不阻止用户保存

### 6.4 账单详情与编辑页

页面目标：查看账单完整信息，并修改错误记录。

展示内容：

- 类型、金额、日期、分类
- 商户或备注
- 关联图片
- 创建和更新时间

支持操作：

- 编辑全部可修改字段
- 更换或删除关联图片
- 删除整笔账单

删除账单前必须二次确认，删除后不可恢复。

## 7. 核心用户流程

### 7.1 手动记账

```text
首页
  -> 点击新建
  -> 选择手动记账
  -> 输入金额
  -> 选择日期和分类
  -> 可选填写备注和图片
  -> 保存
  -> 返回首页
```

### 7.2 图片记账

```text
首页
  -> 点击新建
  -> 选择图片记账
  -> 选择或拍摄图片
  -> 等待识别
  -> 检查或修改识别结果
  -> 保存
  -> 返回首页
```

### 7.3 修改账单

```text
首页
  -> 点击账单
  -> 点击编辑
  -> 修改字段
  -> 保存
```

### 7.4 删除账单

```text
账单详情
  -> 点击删除
  -> 二次确认
  -> 删除账单和关联图片
  -> 返回首页
```

## 8. 页面状态

### 8.1 首页状态

| 状态 | 展示方式 |
| --- | --- |
| 无账单 | 空状态提示和新建入口 |
| 有账单 | 正常列表和汇总 |
| 数据加载失败 | 错误提示和重试入口 |

### 8.2 图片识别状态

| 状态 | 展示方式 |
| --- | --- |
| 上传中 | 图片预览和进度提示 |
| 识别中 | 明确的处理状态 |
| 识别成功 | 可编辑表单回填结果 |
| 识别失败 | 保留图片并允许手动录入 |
| 图片过大或格式错误 | 明确提示支持的格式和大小 |

### 8.3 保存状态

| 状态 | 规则 |
| --- | --- |
| 保存中 | 禁止重复提交 |
| 保存成功 | 返回首页并更新汇总 |
| 保存失败 | 保留表单数据并展示错误信息 |

## 9. 数据模型

### 9.1 Transaction

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | UUID | 主键 |
| type | enum | income 或 expense |
| amount_minor | integer | 以分为单位存储 |
| currency | string | 固定 CNY |
| occurred_at | datetime | 账务发生日期 |
| category_id | UUID | 关联分类 |
| note | string | 备注，可空 |
| receipt_id | UUID | 关联图片，可空 |
| created_at | datetime | 创建时间 |
| updated_at | datetime | 更新时间 |

### 9.2 Receipt

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | UUID | 主键 |
| file_name | string | 原文件名 |
| mime_type | string | 图片类型 |
| size_bytes | integer | 文件大小 |
| storage_key | string | 本地存储标识 |
| image_hash | string | 文件哈希，用于后续查重 |
| raw_text | text | 原始识别文本 |
| recognized_fields | JSON | 结构化识别结果 |
| confidence | number | 整体置信度 |
| created_at | datetime | 创建时间 |

### 9.3 Category

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | UUID | 主键 |
| name | string | 分类名称 |
| type | enum | income 或 expense |
| icon | string | 图标标识 |
| color | string | 展示颜色 |
| sort_order | integer | 排序 |
| is_builtin | boolean | 是否内置分类 |

### 9.4 AppSetting

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| key | string | 设置键 |
| value | JSON | 设置值 |
| updated_at | datetime | 更新时间 |

## 10. 业务规则

1. 金额统一使用最小货币单位整数存储，展示时转换为两位小数。
2. 每笔账单必须包含类型、金额、发生日期和分类。
3. 备注为选填项，最大长度为 200 个字符。
4. 图片为选填项，每笔账单最多关联一张图片。
5. 删除账单时同时删除关联图片。
6. 图片识别结果不能直接落库，必须经过用户确认。
7. 识别服务不可用时，图片记账自动降级为手动补录。
8. 所有本地时间统一按用户设备所在时区处理。

## 11. 图片识别设计

### 11.1 识别输入

- 压缩后的图片
- 可选提示：当前用户所在地区和默认币种

### 11.2 识别输出

```json
{
  "amount": "36.50",
  "currency": "CNY",
  "occurred_at": "2026-09-10",
  "merchant": "示例商户",
  "category": "餐饮",
  "confidence": 0.92
}
```

### 11.3 识别规则

- 金额缺失时允许用户手动填写
- 日期无法识别时默认使用今天
- 币种无法识别时默认使用 CNY
- 分类建议不能确定时使用“其他支出”
- 置信度过低时提示用户重点检查金额和日期

### 11.4 降级方案

识别服务超时、返回错误或不支持当前图片时：

1. 保留已上传图片
2. 进入手动记账表单
3. 图片作为附件随账单保存

## 12. 视觉与交互原则

### 12.1 整体风格

界面应安静、清晰、工具化，避免装饰性卡片和复杂动效。

- 移动端优先
- 信息密度适中
- 金额和汇总数据优先可见
- 按钮和图标具有明确功能
- 使用系统常见记账符号和图标

### 12.2 关键视觉规则

- 支出使用红色系强调
- 收入使用绿色系强调
- 正文使用高对比度中性色
- 图片识别过程中避免遮挡图片预览
- 删除等危险操作需要明确确认

## 13. 非功能要求

### 13.1 性能

- 首页数据加载应在 1 秒内完成
- 手动记账从进入页面到保存完成不超过 30 秒
- 图片从上传到保存完成不超过 60 秒

### 13.2 数据安全

- 数据和图片默认仅保存在用户设备
- 图片识别使用加密网络传输
- 不向统计或广告服务上传账务数据
- 删除账单后同步清理关联图片

### 13.3 可用性

- 无网络时仍可手动记账和查看历史账单
- 移动端和桌面端均可完成核心流程
- 输入错误时提供明确反馈

### 13.4 可访问性

- 表单字段有明确标签
- 颜色不是唯一状态提示
- 按钮具备可操作区域
- 键盘可以完成主要表单操作

## 14. 测试与交付标准

### 14.1 测试范围

- 金额计算和统计逻辑单元测试
- 手动记账表单组件测试
- 图片识别结果回填测试
- 识别失败降级流程测试
- 本地数据持久化测试
- 手动和图片记账核心流程端到端测试

### 14.2 交付标准

满足以下全部条件后才可交付：

- `MVP_SCOPE.md` 中所有 MVP 验收标准通过
- 手动记账和图片记账两条核心流程可完整运行
- 数据在刷新或重启后仍存在
- 所有自动化测试通过
- 无阻断级缺陷
- 代码和文档均创建对应 Git commit

## 15. 待确认决策

以下决策建议在技术方案开始前确认，当前默认值仅供设计：

| 决策项 | 当前默认建议 | 说明 |
| --- | --- | --- |
| 前端形态 | Web/PWA | 可快速验证，后续可封装 |
| 本地存储 | IndexedDB | 保存结构化数据和图片 |
| 图片识别 | 视觉模型或 OCR 服务 | 需要确认成本和准确率 |
| 后端部署 | MVP 先不部署 | 仅图片识别调用外部服务 |
| 图片保留策略 | 默认永久保留 | 后续再提供清理策略 |

## 16. 后续路线

第二阶段补充多币种、汇率、账户、筛选、导出和基础图表。

第三阶段再考虑预算、周期账单、云同步、多用户和复杂报表。
